# 安全策略

## 支持版本

GVideo 当前处于 `1.0` 之前的开发阶段，安全修复只应用于最新默认分支。

## 报告安全漏洞

涉及认证、授权、CSRF、Cookie、私密媒体访问、上传校验、命令执行或数据泄露的问题，不要提交公开 Issue。

GitHub 仓库启用 Private Vulnerability Reporting 后，请通过私密漏洞报告提交，并包含：

- 受影响版本或 Commit
- 可复现步骤
- 预期与实际行为
- 潜在影响
- 可选的缓解建议

报告中不得附带真实用户凭证、私密媒体、数据库、访问令牌或其他敏感数据。

## 安全要求

- 生产部署必须使用 HTTPS，并设置 `COOKIE_SECURE=true`。
- 密钥与部署凭证只能由环境或平台 Secret 注入。
- 上传媒体必须继续通过大小、扩展名和真实文件头校验。
- 私密视频、字幕、封面、HLS 与原视频鉴权必须成组测试。
- Cloudflare Quick Tunnel 只是临时验收工具，不是生产托管方案。
